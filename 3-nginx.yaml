- name: install nginx
  hosts: web
  become: yes
  tasks:
  - name: nginx package installation
    ansible.builtin.package:
    - name: nginx
      state: present
  - name: enable nginx
    ansible.builtin.service:
    - name: nginx
      state: started
      enabled: yes