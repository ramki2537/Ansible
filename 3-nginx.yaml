- name: nginx installation
  hosts: web
  become: yes
  tasks:
  - name: settingup nginx
    ansible.builtin.package:
    - name: nginx
      state: present
  - name: nginx service
    ansible.builtin.service:
    - name: nginx
      state: started
      enabled: yes